## Start server

```bash
# build tsc -> js
$ yarn build
# start the server
$ yarn start
```

## Run Local Postgres

```bash
$ docker run --name territory-postgres -e POSTGRES_PASSWORD=mysecretpassword -d postgres
```
